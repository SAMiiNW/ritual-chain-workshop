# Copy to ".env" and fill in a SEPARATE testnet wallet (different from any other).
#   copy .env.example .env
#
# Ritual Chain (chainId 1979) TESTNET private key, funded from the faucet:
#   https://faucet.ritualfoundation.org
# THROWAWAY TESTNET KEY ONLY. Never a real/mainnet key.
PRIVATE_KEY=0xput_your_separate_testnet_key_here
RPC_URL=https://rpc.ritualfoundation.org
